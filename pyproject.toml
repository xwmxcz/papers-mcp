[project]
name = "papers-mcp"
version = "0.1.0"
description = "Academic Papers MCP Server - 搜论文/查引用/读PDF"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "arxiv>=2.1",
    "httpx>=0.24",
    "PyMuPDF>=1.23",
]

[project.scripts]
papers-mcp = "server:mcp.run"
